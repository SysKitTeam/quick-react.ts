import * as React from 'react';

export interface IFarmHeaderProps {
    sharepointVersion: string;
    sharepointIcon: string;
    configDB: string;
    configDBIcon: string;
    className?: string;
    isCustomFarm: boolean;
}
